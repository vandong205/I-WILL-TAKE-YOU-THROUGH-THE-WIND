init python:
    class bubble_speech(object):
        def __init__(self, type, padx=0, pady=0, textsize=0, textcolor="#000000"):
            self.type = str(type)
            self.padx = int(padx)
            self.pady = int(pady)
            self.textsize = int(textsize)
            self.textcolor = str(textcolor)
